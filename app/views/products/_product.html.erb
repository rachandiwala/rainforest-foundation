<!-- code moved from index.html.erb, products main page -->

<!-- renders a helpful message with numbers of displayed vs. total entries -->

<div class="product-list">

  </div>

    
  <div class="image">

<% @products.each do |product| %>

  <%= product.name %>,   $<%= product.formatted_price %> 

  <%= link_to image_tag(product.url), "/products/#{product.id}" %>


  <% end %>
  </div>

<!-- keeps the description to 100 characters or less 
  <p><%#= truncate(product.description, length: 100) %></p>

-->

<!-- outputs several pagination links such as « First ‹ Prev ... 2 3 4 5 6 7 8 9 10 ... Next › Last » -->

<!--   code to create a link out of product name with path to the prodcut 

  <%#= link_to product_path(product.id) do %>
  <h2>
    <%= product.formatted_price %>
    <%= product.name %>
    <%= product.description %>
  </h2>
<%# end %>

-->